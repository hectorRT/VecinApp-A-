export class Discusion {
    constructor(public idDiscusion: number, public idVecino: number, public titulo: string, public descripcion: string, public estatus: string, public fecha: string) {

    }
}